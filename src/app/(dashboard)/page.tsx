import React from 'react';

const Dashboard = () => {
	return (
		<div className="text-4xl text-purple-700 font-bold ">
			This is an authenticated dashboard page , only logged in users can see it
		</div>
	);
};

export default Dashboard;
